from fastapi import APIRouter

router = APIRouter(prefix="/validations", tags=["validations"])

# Assign a license to a product 

# Get all validations 

# Update the validation

# Delete the validation if nothing assigned to it