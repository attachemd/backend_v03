from fastapi import APIRouter

router = APIRouter(prefix="/validators", tags=["validators"])

# Assign a license to a product 

# Get all validators 

# Update the validator

# Delete the validator if nothing assigned to it