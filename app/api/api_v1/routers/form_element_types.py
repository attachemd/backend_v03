from fastapi import APIRouter

router = APIRouter(prefix="/form_element_types", tags=["form_element_types"])

# Create form element type

# Get all form element types

# Delete the form element if not used
