from fastapi import APIRouter

router = APIRouter(
    prefix="/selected_form_element_list_values",
    tags=["selected_form_element_list_values"],
)

# Assign a value from (form element list value) to (filled form)

# Update the assignment of filled form

# Delete the assignment of filled form
