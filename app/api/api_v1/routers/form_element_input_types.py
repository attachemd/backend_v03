from fastapi import APIRouter

router = APIRouter(prefix="/form_element_input_types", tags=["form_element_input_types"])

# Assign a license to a product 

# Get all form_element_input_types 

# Update the form_element_input_type

# Delete the form_element_input_type if nothing assigned to it