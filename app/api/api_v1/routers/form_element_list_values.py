from fastapi import APIRouter

router = APIRouter(prefix="/form_element_list_values", tags=["form_element_list_values"])

# Create a form element list value one row at a time and
# assign a form element to it.

# Update form element list value

# Delete all the list related to a form element.
